# Финансовый помощник для самозанятых

Независимый статический сайт с калькуляторами для самозанятых (НПД-плательщиков) в РФ.
Расчёты выполняются в браузере, данные не передаются на сервер.

**Production:** https://samozanyatye.vercel.app/

## 🚀 Быстрый старт

### Локальный запуск
Откройте `index.html` в любом браузере:
```bash
open index.html  # macOS
# или просто перетащите файл в браузер
```

### Через локальный сервер
```bash
# Python 3
python3 -m http.server 8000

# Node.js (npx)
npx serve .

# Затем откройте http://localhost:8000
```

### Тесты
```bash
npm test
```

## 📁 Структура
```
self-employed/
├── index.html              # Главная страница
├── bolnichny.html          # Калькулятор взносов на больничный
├── limit-dohoda.html       # Калькулятор лимита дохода
├── ip-ili-samozanyatyy.html# Сравнение НПД и ИП
├── pensiya.html            # Калькулятор пенсионного стажа
├── koncentracia-dohoda.html# Трекер концентрации дохода
├── otlozhit-na-nalog.html  # Калькулятор «Отложить на налог»
├── stavka-v-chas.html      # Калькулятор ставки в час
├── dogovor-akt.html        # Генератор договора и акта
├── master-vybora-rezhima.html # Мастер выбора режима
├── poleznoe.html           # Статьи и FAQ
├── metodika.html           # Методика и источники
├── privacy.html            # Политика конфиденциальности
├── disclaimer.html         # Ограничение ответственности
├── css/                    # Стили
├── js/
│   ├── rules-2026.js       # Единый файл нормативных параметров
│   ├── calculators.js      # Логика калькуляторов
│   ├── tax-constants.js    # Адаптер для мастера выбора режима
│   ├── wizard.js           # Мастер выбора режима
│   ├── components.js       # UI-компоненты
│   ├── controls.js         # Элементы управления
│   └── theme.js            # Переключатель темы
├── tests/
│   └── calculations.test.js # Unit-тесты расчётов
├── robots.txt
├── sitemap.xml
├── package.json
└── README.md
```

## 🛠 Развёртывание

### Vercel
```bash
npm i -g vercel
vercel
```

### Netlify
Перетащите папку проекта в Netlify Drop или подключите репозиторий.

## 📊 Калькуляторы

- **Взносы на больничный** — тариф 3,84%, страховые суммы 35 000 / 50 000 ₽, скидки
- **Лимит дохода** — остаток до 2 400 000 ₽, безопасный темп, прогноз превышения
- **Самозанятый или ИП** — сравнение НПД и УСН 6% без работников
- **Мастер выбора режима** — 4 вопроса для рекомендации режима
- **Пенсионный стаж** — стоимость добровольных взносов ОПС
- **Концентрация дохода** — риск переквалификации при зависимости от одного клиента
- **Отложить на налог** — расчёт с учётом частичного вычета 10 000 ₽
- **Ставка в час** — целевая и фактическая ставка с учётом НПД
- **Договор и акт** — генератор шаблона с экранированием ввода

## ⚙️ Ежегодное обновление нормативных данных

1. Создайте `js/rules-2027.js` по аналогии с `rules-2026.js`
2. Обновите ставки, лимиты, МРОТ-зависимые суммы по официальным источникам (ФНС, СФР)
3. Подключите новый файл на HTML-страницах вместо старого
4. Обновите `updatedAt` в файле конфигурации
5. Обновите даты на странице `metodika.html`
6. Запустите тесты: `npm test`
7. При необходимости поднимите версию в `package.json`

Все нормативные параметры находятся в едином файле — дублирования нет.

## 📝 Тестирование

### Автоматические тесты
```bash
npm test
```
Покрывают: НПД для физлиц и организаций, частичное применение вычета, лимит дохода,
сравнение НПД и ИП, почасовую ставку, пенсионный стаж, больничный, концентрацию дохода,
экранирование HTML, edge cases.

## 📄 Лицензия

Проект создан для образовательных целей. Не является официальным сервисом ФНС или СФР.
